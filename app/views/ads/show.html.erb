<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @ad.title %>
        <%= image_tag @ad.preview.url %>
</p>

<p>
  <strong>Url:</strong>
  <%= @ad.url %>
</p>

<%= link_to 'Edit', edit_ad_path(@ad) %> |
<%= link_to 'Back', ads_path %>
